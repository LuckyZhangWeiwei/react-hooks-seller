.goods-container
  display: flex
  height: 100%;
  overflow: hidden;
  position: relative;
  .goods-nav
    height: 100%;
    order: -1;
    position: relative;
    overflow: hidden;
    white-space: normal;
    width: 80px;