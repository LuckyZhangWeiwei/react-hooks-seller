.goods-container
  display: flex
  height: 100%;
  overflow: hidden;
  position: relative;
  bottom: 45px
  .goods-nav
    height: 100%;
    order: -1;
    position: relative;
    overflow: hidden;
    white-space: normal;
    width: 80px;
  .goods-panel
    flex: 1;
    height: 100%;
    overflow: hidden;
    position: relative;
    z-index: 1;